# kubernetes/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: sre-service
spec:
  type: ClusterIP
  selector:
    app: sre-app
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30007
